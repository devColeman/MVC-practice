<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Post</title>
    <link rel="stylesheet" href="/css/styles.css">
    <script src="/js/main.js" defer></script>
</head>
<body>
    <h1>Blog Post</h1>
    <a href="/">home</a>
    <a href="/add">add a post</a>
    
  
   <% blogItems.forEach((blog, i) => { %>
       
         <div id="<%= blogItems[i].title %>">
            <span> <%= blogItems[i]%></span>
            <h2><%= blog.title %></h2>
            <p><%= blog.content %></p>
            <p id="likes">♡ <%= blog.likes %></p>
            <p>By: <%= blog.author %></p>
         </div>
    <% }) %> 


    

    // add public folder for the div then add if there is no blogsitems also add a linke to add post,
    // once these are done we can start looking into comments
</body>
</html>